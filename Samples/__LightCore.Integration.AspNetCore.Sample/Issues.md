* Threading issue is related to the tests
  * Test setup is done kinda wrong
* ASP.NET Core Integration doesn't work because of an missing feature in LightCore
  * We need to support dynamic mappings
  * ​